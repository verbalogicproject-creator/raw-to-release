# Sanitized observations

Store aggregated compatibility observations here only after empirical runs.
Never commit transcripts, prompts containing private source, credentials,
personal data, or unnecessary absolute paths. An empty directory means runtime
acceptance remains unverified.
