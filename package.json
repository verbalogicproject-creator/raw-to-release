{
  "name": "raw-to-release-tooling",
  "private": true,
  "version": "0.1.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build-r2rctl.mjs",
    "check:bundle": "node scripts/build-r2rctl.mjs --check",
    "check:schemas": "node scripts/validate-schemas.mjs",
    "check:supply-chain": "node scripts/check-supply-chain.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "@types/node": "22.15.3",
    "ajv": "8.20.0",
    "typescript": "5.8.3"
  }
}
